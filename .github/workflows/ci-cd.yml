name: CI/CD Pipeline for Docker Deployment

# Trigger the workflow when code is pushed to the main branch
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  # Build and push Docker image
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Cache Docker layers
      uses: actions/cache@v3
      with:
        path: /tmp/.buildx-cache
        key: ${{ runner.os }}-buildx-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-buildx-

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: |
        docker build -t sandeeppalbrs/myapi:${{ github.sha }} .

    - name: Push Docker image to Docker Hub
      run: |
        docker push sandeeppalbrs/myapi:${{ github.sha }}

  # # Deploy the Docker image to a remote server
  # deploy:
  #   needs: build
  #   runs-on: ubuntu-latest

  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v2

  #   - name: SSH to server and deploy
  #     uses: appleboy/ssh-action@v0.1.1
  #     with:
  #       host: ${{ secrets.HOST }}
  #       username: ${{ secrets.USERNAME }}
  #       password: ${{ secrets.PASSWORD }}
  #       script: |
  #         docker pull yourusername/myapi:${{ github.sha }}
  #         docker stop myapi-container || true
  #         docker rm myapi-container || true
  #         docker run -d -p 80:80 --name myapi-container yourusername/myapi:${{ github.sha }}
